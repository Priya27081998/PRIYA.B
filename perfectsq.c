#include<stdio.h>
int main()
{
int a,b;
printf("enter two num:");
scanf("%d %d",&a,&b);
printf("product is:%d",a*b);
if(a/b=1)
{
 printf("perfect square");
 }
 else
 {
 printf("not perfect square");
 }
 return 0;
 }
